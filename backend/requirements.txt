# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0      # ASGI server that runs FastAPI

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30             # ORM — lets us write Python instead of raw SQL
asyncpg==0.29.0                # async PostgreSQL driver
alembic==1.13.1                # database migrations

# ── Redis ─────────────────────────────────────────────────────────────────────
redis==5.0.4                   # async Redis client (queue + pub/sub)

# ── Validation ────────────────────────────────────────────────────────────────
pydantic==2.7.1                # data validation for API schemas
pydantic-settings==2.2.1       # reads settings from .env file

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1           # loads .env file into environment variables
httpx==0.27.0                  # async HTTP client (for testing + external calls)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7         # lets us write async test functions
aiosqlite==0.20.0              # in-memory SQLite for fast tests (no Postgres needed)
